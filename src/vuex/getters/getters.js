export default {
    PRODUCTS(state) {
      return state.products;
    },
    CART(state) {
        return state.cart;
    },
    ACCOUNT(state) {
        return state.account
    },
    ORDER_DATA(state) {
        return state.orderData
    }
}